---
type Props = {
  date: Date;
};

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
  {
    date.toLocaleDateString('no-NB', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
